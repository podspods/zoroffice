import {Box} from '@mui/material';
import {styled, Theme} from '@systran/react-components/lib/Theme';

export const TemporaryPageBox = styled(Box) < {theme?: Theme}>`
  width: 100%;
  margin: 2rem;
  overflow-x: hidden;
`;
